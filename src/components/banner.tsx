import styles from "../style/banner.module.scss";

const Banner = () => {
  return (
    <section className={styles.bannerSection}>
      <h2>Heilung in Händen, Sorge im Herzen</h2>
    </section>
  );
};

export default Banner;
